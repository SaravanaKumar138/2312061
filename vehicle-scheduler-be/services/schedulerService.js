
const taskService = require("./taskService");
exports.schedule = async(depotId) => {
    const tasks = await taskService.getTasks(depotId);
const maxhouse = 0;
const result   = knapsack(tasks.maxhouse);
return result; 
}